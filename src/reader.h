#include <stdlib.h>

char *readLine ( FILE *file_ptr );

int getPoint ( FILE *file_ptr );

//void appendToFile ( char *fileName, char *line );

distance Distance ( Point P1, Point P2 );

char *cliHandler ( int argc, char* argv[] );

#include "./reader.c"
